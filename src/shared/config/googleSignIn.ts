/**
 * Web client ID from the Firebase console (Project settings > General >
 * "Web client (auto created by Google Service)" under OAuth 2.0 client IDs).
 * Required by `GoogleSignin.configure()` to obtain an ID token that Firebase
 * Auth accepts. Fill in once the Firebase project exists.
 */
export const GOOGLE_WEB_CLIENT_ID = '';
